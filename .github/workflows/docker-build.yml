name: Docker Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  # Allow manual triggering
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Build Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: false
          tags: monaco-editors-iframe:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Uncomment this job if you want to push to a registry
  # push:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   # Only run on pushes to main/master, not on PRs
  #   if: github.event_name == 'push'
  #   
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3
  #     
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v2
  #     
  #     - name: Login to GitHub Container Registry
  #       uses: docker/login-action@v2
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #     
  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v4
  #       with:
  #         context: .
  #         push: true
  #         tags: |
  #           ghcr.io/${{ github.repository_owner }}/monaco-editors-iframe:latest
  #           ghcr.io/${{ github.repository_owner }}/monaco-editors-iframe:${{ github.sha }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max 